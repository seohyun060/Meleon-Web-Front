import React from 'react';
import VideoRecommend from '../components/VideoRecommend';

const VideoRecommendContainer = () => {
  return <VideoRecommend />;
};

export default VideoRecommendContainer;
